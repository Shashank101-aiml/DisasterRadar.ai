import React from 'react';
import PlaceholderScreen from '../components/shared/PlaceholderScreen';

export default function PredictRiskScreen() {
  return (
    <PlaceholderScreen
      title="Predict Risk Studio"
      phase="Phase 2"
      description="Location search, scenario presets, 5 live-recompute sliders, and the XGBoost vs Random Forest Compare Models panel — built in Phase 2."
    />
  );
}
