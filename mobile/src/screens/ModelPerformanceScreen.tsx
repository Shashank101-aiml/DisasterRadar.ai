import React from 'react';
import PlaceholderScreen from '../components/shared/PlaceholderScreen';

export default function ModelPerformanceScreen() {
  return (
    <PlaceholderScreen
      title="Model Performance Studio"
      phase="Phase 4"
      description="Model selector, live threshold recompute, ROC/PR curves, SHAP explorer, and multi-model radar chart, all rebuilt with react-native-svg — built in Phase 4."
    />
  );
}
