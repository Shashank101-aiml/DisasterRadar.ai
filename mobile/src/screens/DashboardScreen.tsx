import React from 'react';
import PlaceholderScreen from '../components/shared/PlaceholderScreen';

export default function DashboardScreen() {
  return (
    <PlaceholderScreen
      title="Executive Dashboard"
      phase="Phase 2"
      description="Donut risk gauge, editable parameter table, SHAP risk-factor bars, live station map, and recent predictions feed — built in Phase 2."
    />
  );
}
