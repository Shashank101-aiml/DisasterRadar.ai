import React from 'react';
import PlaceholderScreen from '../components/shared/PlaceholderScreen';

export default function EvacuationScreen() {
  return (
    <PlaceholderScreen
      title="Offline Evacuation & Roads"
      phase="Phase 3"
      description="Real MapLibre offline region caching, shelters/road-blockages, geolocation, and SOS clipboard — replacing the web version's simulated downloader, built in Phase 3."
    />
  );
}
