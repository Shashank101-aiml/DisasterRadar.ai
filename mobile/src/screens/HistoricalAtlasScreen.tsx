import React from 'react';
import PlaceholderScreen from '../components/shared/PlaceholderScreen';

export default function HistoricalAtlasScreen() {
  return (
    <PlaceholderScreen
      title="Historical Disaster Atlas"
      phase="Phase 2"
      description="KPI cards, searchable/filterable disaster event table, and CSV export via the native share sheet — built in Phase 2."
    />
  );
}
